{
  "name": "learnspace-effect",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "basic": "npx tsgo --noEmit && node --experimental-strip-types src/basic/index.ts",
    "typecheck": "npx tsgo --noEmit",
    "lint": "npx oxlint src",
    "fmt": "npx oxfmt --write src",
    "fmt:check": "npx oxfmt src",
    "check": "npx tsgo --noEmit & npx oxlint src & npx oxfmt src & wait"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "effect": "4.0.0-beta.64"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260508.1",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0"
  }
}
